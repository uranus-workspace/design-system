/** Known OAuth/OIDC identity providers accepted by Uranus apps. Extend with `'generic'` for custom redirects. */
export type OAuthProviderId = 'apple' | 'generic' | 'github' | 'google' | 'microsoft';
